function guess() {
    const userInput = document.getElementsByName("userGuess"); 
    const correct = Math.floor(Math.random() * 10) + 1;
    let guesses = [];
    let wins = 0;
    let losses = 0;
}